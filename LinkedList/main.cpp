#include "LinkedList.hpp"

int main(){
    List l;
    for(int i=0; i<5; ++i){
        l.insert_back(x);
    }
    Node *head = l;
    l.displayList();
    l.reverse_list(head);
    l.displayList();
}