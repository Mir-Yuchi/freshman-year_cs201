#include <bits/stdc++.h>
using namespace std;

vector<vector<int>> largeGroupPositions(string s) {
    map<char, int> freq;
    for(char i : s){
        freq[i]++;
    }

    for(auto it: freq){
        if (it.second>= 3
    }
}

int main(){

}